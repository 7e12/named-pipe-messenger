# named-pipe-messenger
A messenger example based on a named pipe (FIFO)
## Execute
```$bash
$ sh setup.sh [options] main1.c main2.c
OPTIONS:
    -l              Execute on Linux
    -m              Execute on Mac
```
